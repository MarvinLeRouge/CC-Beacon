# OS
.DS_Store
Thumbs.db

# IDE
.fleet
.idea
.nova
.vscode
.zed

# Logs
*.log

# Git keepfile helper
!.gitkeep

# Safety net — never commit a real config with secrets
config.json

# CC-Beacon planning notes (work in progress, not part of the versioned project)
docs/work-in-progress

# Claude Code session metadata
CLAUDE.md
