# Docker Compose variable substitution — values here are NOT passed to containers
# Copy to compose/.env on the VPS and fill in the real domain
DOMAIN=your-domain.com

# Image tag to deploy — set to a specific commit SHA tag by the CI/CD pipeline
# on every deploy. Defaults to "latest" for manual/local startup.
IMAGE_TAG=latest
